import type { Recipe } from "../../../src/index";
export declare function RecipeSidebarApp({ initialRecipe, onClose, isMobile, }: {
    initialRecipe: Recipe;
    onClose: () => void;
    isMobile?: boolean;
}): import("preact").JSX.Element;
